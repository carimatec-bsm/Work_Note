# Work Note

GitHub Pages에서 실행되는 로컬 전용 Work Note 정적 배포본입니다.

- 앱 경로: `react/`
- Google Drive 및 서버 동기화 기능은 포함하지 않습니다.
- 업무 데이터와 첨부 원본은 각 브라우저의 `localStorage`와 `IndexedDB`에만 저장됩니다.
- JSON, ZIP, CSV, Excel 백업과 첨부 파일은 저장소에 커밋하지 않습니다.

저장소에는 검증된 정적 배포 파일만 포함합니다.
